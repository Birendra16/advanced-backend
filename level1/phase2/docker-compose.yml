services:
  backend:
    build: ./backend
    env_file:
      - ./backend/.env
    ports:
      - 8001:7000
  
  frontend:
    build: ./frontend
    ports:
      - 3000:3000

  redis:
    image: redis
    ports:
      - "6379:6379"
